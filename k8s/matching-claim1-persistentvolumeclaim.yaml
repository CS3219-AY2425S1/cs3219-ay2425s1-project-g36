apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: matching-claim1
  name: matching-claim1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
